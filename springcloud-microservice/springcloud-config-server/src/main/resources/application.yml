server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pattabhia/springcloud-configuration
          search-paths: 'station*'
        encrypt:
          enabled: false
  security:
    user:
      name: pattabhi
      password: welcome