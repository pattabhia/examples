node {
  stage('Clone repo')
    checkout scm
}

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn install package'  
                archiveArtifacts artifacts: '**/target/*.jar' 
            }
        }
    }
}
