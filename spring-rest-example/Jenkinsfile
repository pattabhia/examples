node {

  def rtMaven = Artifactory.newMavenBuild()

  def buildInfo
    stage('Clone repo')
      checkout scm

  stage('Maven build') {
        buildInfo = rtMaven.run pom: 'spring-rest-example/pom.xml', goals: 'clean install'
    }
  

}
