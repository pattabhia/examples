pipeline {
    agent any

    stages {

        stage('Clone repo') {
          steps {
            checkout scm
          }
        }

        stage('build spring-rest-example') {
            steps {
                sh 'mvn clean'
            }
        }
    }
}
