pipeline {
    agent any

    stages {

        stage('Clone repo') {
          steps {
            checkout scm
          }
        }

        stage('clean install') {
            steps {
                sh 'mvn -f spring-rest-example/pom.xml clean install'
            }
        }
    }
}
